package Repl_And_Practice;

import java.util.Scanner;

public class Task110 {
    public static void main(String[] args) {


        //Given a String array words, iterate through each word and print first and last letter
        // of each element in separate lines.
        //
        //Example:
        // words → ["hello", "why", "by", "apple" , "note"]
        // prints :ho
        //       wy
        //       by
        //       ae
        //       ne

        Scanner input = new Scanner(System.in);
        String[] words = {input.nextLine(),input.nextLine(),input.nextLine(),
                input.nextLine(),input.nextLine()};

            int lastCharIndex= words[0].length()-1;

    //    for (int i = 0; i <lastCharIndex ; i++) {
      //      String firstLast = words[i].substring(0,


        }
        }


     //   System.out.println(name.substring(name.charAt(0)+1,name.length()-2 ));









